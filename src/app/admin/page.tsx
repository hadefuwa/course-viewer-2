// Admin CMS dashboard — course list, import from Google Sheet / Excel, cache management
// Requires admin login (Supabase Auth)
// TODO: implement auth guard + course management UI
export default function AdminPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-3xl font-bold mb-4">Admin</h1>
      <p className="text-gray-400 italic">CMS dashboard — coming soon.</p>
    </main>
  )
}
